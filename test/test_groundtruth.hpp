
#include "gtest/gtest.h"


#include <pcl/point_cloud.h>
#include <pcl/point_types.h>

#include "camera_model_ortho.h"
#include "solver.hpp"
//#include "groundtruth_data.hpp"

//using namespace mrf;
//using Point = pcl::PointXYZI;
//using Cloud = pcl::PointCloud<Point>;



TEST(MRF,loadGT) {

    /** load Data
     *
     */
//    const Cloud::Ptr cloud_dense{new Cloud};
//    const Cloud::Ptr cloud_sparse{new Cloud};
//
//    /**
//     * Load GT Data
//     */
//    cv::Mat image;
//    loadCameraImage(image);
////    const int width{image.cols};
////    const int height{image.rows};
////    const int dim{width * height};
//    cv::Mat gt_depth_image;
//    GroundTruthParams params_gt;
    //Eigen::VectorXd gt_depth{Eigen::VectorXd::Zero(dim)};
    //loadDenseCloud(cloud_dense);
    //loadCloudSparse(cloud_dense, cloud_sparse, width, height, params_gt);

    /**
    * Solver
    */
//    CameraModelOrtho cam(width,height);
//    Parameters params;
//    Eigen::Affine3d transform;


    //    Eigen::Affine3d transform;
    //    Data data(cloud_sparse,image,transform);
    //
    //    Solver solver(cam,params);
    //
    //    bool succes;
    //    success = solver.solve(data);

}
